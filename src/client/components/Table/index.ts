import { Head, Row, Cell, Body, TableHead, Paginator, TableButtons } from "./components"
import Table from "./Table"

export { Head, Body, Row, Cell, Paginator, TableHead, TableButtons }
export default Table
